# Why CW Cannot Measure Range

Range from time-of-flight requires observing transmission time reference and return delay.

Pure CW has constant transmission with no identifiable start time per cycle, so delay cannot be uniquely mapped to distance.
