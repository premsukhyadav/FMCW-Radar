# Velocity Resolution

Velocity resolution depends on coherent processing interval (CPI):

\[ \Delta v \propto \frac{\lambda}{2\,\text{CPI}} \]

More chirps per frame (longer CPI) improves velocity resolution but can reduce temporal responsiveness.
